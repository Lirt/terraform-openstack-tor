# Usage

```bash
./provision_tor.sh "${SSH_PRIV_KEY_LOCATION}" \
                   "${ANSIBLE_PLAYBOOK_USER}" \
                   "${TOR_FIP}" \
                   "${ANSIBLE_RELAYOR_PLAYBOOK_PATH}"
```
